{
  "name": "ppqa",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "private": true,
  "workspaces": [
    "packages/qa-framework"
  ],
  "scripts": {
    "dev-setup": "npx playwright install --with-deps && yarn run build",
    "build": "npx lerna run build && yarn install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.22.0",
    "lerna": "^4.0.0",
    "qa-framework": "./packages/qa-framework"
  },
  "dependencies": {
    "playwright": "^1.22.1"
  }
}
