{
  "name": "ppqa",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "workspaces": [
    "packages/qa-framework"
  ],
  "scripts": {
    "dev-setup": "yarn install --frozen-lockfile && npx playwright install --with-deps && yarn run build",
    "build": "npx lerna run build && npm i && npm i"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.22.0",
    "lerna": "^4.0.0",
    "qa-framework": "./packages/qa-framework"
  },
  "dependencies": {
    "playwright": "^1.22.1"
  }
}
