{
  "name": "ppqa",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "private": true,
  "workspaces": [
    "packages/qa-framework/*"
  ],
  "scripts": {
    "dev-setup": "yarn config set workspaces-experimental true && npx playwright install --with-deps && yarn run build",
    "build": "npx lerna run build && yarn install && yarn --check-files",
    "webapp": "npx lerna run dev --scope=@qa-framework/tauri-gui"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.22.0",
    "lerna": "^4.0.0",
    "@qa-framework/playwright-ui": "./packages/qa-framework/playwright-ui"
  },
  "dependencies": {
    "playwright": "^1.22.1"
  }
}
