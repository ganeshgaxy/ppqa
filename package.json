{
  "name": "ppqa",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "private": true,
  "workspaces": [
    "packages/qa-framework/*"
  ],
  "scripts": {
    "dev-setup": "yarn config set workspaces-experimental true && npx playwright install --with-deps && yarn run build",
    "build": "npx lerna run build --scope=@qa-framework/playwright-ui && yarn remove @qa-framework/playwright-ui -W && yarn install",
    "webapp": "npx lerna run dev --scope=@qa-framework/tauri-gui",
    "tauriapp": "npx lerna run tauri --scope=@qa-framework/tauri-gui"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.22.0",
    "lerna": "^4.0.0"
  },
  "dependencies": {
    "playwright": "^1.22.1"
  }
}
