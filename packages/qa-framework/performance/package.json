{
  "name": "@qa-framework/performance",
  "version": "1.0.0",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "author": "Ganeshan Manickam",
  "license": "MIT",
  "scripts": {
    "prebuild": "cd performance-rust && cargo build --release && cd ..",
    "build": "yarn install && npx tsc && yarn link"
  },
  "dependencies": {
    "ffi-napi": "^4.0.3",
    "tsc": "^2.0.4",
    "typescript": "^4.7.4"
  },
  "devDependencies": {
    "@types/ffi-napi": "^4.0.5"
  }
}
